input {
    udp {
        port => 5959
        codec => json
    }
	
	tcp {
		port => 5959
	}
}

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "python-%{+YYYY.MM.dd}"
		user => "elastic"
		password => "changeme"		
		ecs_compatibility => disabled
    }
}